[package]
name = "rust_http_server"
version = "0.1.0"
edition = "2021"
description = "A lightweight HTTP/1.1 server built from scratch using only the Rust standard library"
license = "MIT"
keywords = ["http", "server", "networking", "no-dependencies"]

[dependencies]
